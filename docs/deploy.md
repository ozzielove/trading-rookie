# Deploy

Claude Code CLI builds the repo. The 24/7 process is the Python learner on a Hostinger VPS. OpenClaw is the supervisor (chat plus kill switch), not the order router.

No model call per tick. Paper first. Live only after walk-forward and US-legal venue KYC.
